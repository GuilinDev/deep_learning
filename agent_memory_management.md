我需要重塑你的记忆系统：如下
“”“

我们的核心原则就一条： 文件=事实来源。你不写进文件的东西=你从来不知道的东西。

Agent的记忆不在它的"脑子"里，而在磁盘上。Contextwindow是工作台，文件才是仓库。

这意味着:
所有重要信息必须实时写入文件
Agent每次启动都从文件读取状态
不依赖"记得去检查"，而是靠系统触发(cron、 heartbeat)

本方案的定位
我们选择Markdown文件作为记忆的主要载体，而不是纯数据库方案。我们采取混合方案：Markdown作为事实来源（可读层），QMD向量数据库作为检索加速层。写入Markdown，索引自动同步。

三层架构：
1）短期层：NOW.md，用途是Agent的工作台，记录当前状态，优先级，阻塞项等。特点是每次heartbeat覆写（Write），不追加；只保留当前的完成项，这是compaction的救生筏 - agent压缩上下文后首先读这个文件。示例结构如图。

关键设计，是NOW.md是唯一允许覆写的记忆文件。其它记忆文件只追加，不覆写。
“”“
先完成这步，完成后我们再继续改造


"""
2）中期层：每日日志，用途是事件流水，记录当天发生的一切。是记忆系统的原始数据。文件格式为memory/YYYY-MM-DD.md，写入格式如图。雪茹工具则推荐用一个带自动时间戳的脚本，避免手动编码时间。关键设计是追加式（append-only），永不覆写，不同session看不到彼此的对话，所以重要信息必须立刻写进日志，格式统一为 ### HH:MM:SS - title，便于扫描和检索。
"""

"""
3）长期层：INDEX.md -> 结构化子目录。用途是经过提炼的，结构化的知识库，不是原始事件，而是从事件中抽取的可复用知识。INDEX.md是导航枢纽，只想各个子目录。Agent启动时扫描INDEX.md，按需加载具体文件。如图。关键设计是INDEX.md包含健康度指标（优先级，状态，最后验证日期），Agent扫描时立即判断哪些知识可信；例如红色标记优先级的文件 = 核心知识，永不归档，[stale]标记 = 超过30天未验证，信息可能过时。
"""

三层之间的信息流动如图，信息从上到下逐层提炼，原始对话 ->写入体制（保留细节），日志 -> 提炼到知识库（抽取可复用的教训/决策/画像），过期数据-> 归档到冷存储 （释放检索空间）

所有lessons/ people/ decisions/下的文件必须带YAML frontmatter,如图